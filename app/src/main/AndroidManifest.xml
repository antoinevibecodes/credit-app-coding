<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.applications.tinytonwe.drivermodificationappversion2" >

    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".DriverInfo.DriverInfoActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask" >
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.nfc.action.TECH_DISCOVERED" />
            </intent-filter>

            <meta-data
                android:name="android.nfc.action.TECH_DISCOVERED"
                android:resource="@xml/nfc_tech_filter" />
        </activity>
        <activity
            android:name=".Camera.CameraActivity"
            android:label="@string/title_activity_camera"
            android:launchMode="singleTask"
            android:noHistory="true" >
        </activity>
        <activity
            android:name=".SendPicture.SendPictureActivity"
            android:label="@string/title_activity_validation"
            android:noHistory="true"
            android:parentActivityName=".DriverInfo.DriverInfoActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".DriverInfo.DriverInfoActivity" />
        </activity>
        <activity
            android:name=".SelectOption.OptionsActivity"
            android:label="@string/title_activity_options"
            android:launchMode="singleTask"
            android:parentActivityName=".DriverInfo.DriverInfoActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".DriverInfo.DriverInfoActivity" />
        </activity>
        <activity
            android:name=".ProcessCredits.ProcessCreditsActivity"
            android:label="@string/title_activity_process_credits"
            android:launchMode="singleTop"
            android:parentActivityName=".DriverInfo.DriverInfoActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".DriverInfo.DriverInfoActivity" />
        </activity>
        <activity
            android:name=".Camera.GalleryActivity"
            android:label="@string/title_activity_gallery" >
        </activity>
        <activity
            android:name=".Main.MainActivity"
            android:label="@string/title_activity_main"
            android:launchMode="singleTask" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <intent-filter>
                    <action android:name="android.nfc.action.NDEF_DISCOVERED" />

                    <category android:name="android.intent.category.DEFAULT" />

                    <data android:mimeType="text/plain" />
                </intent-filter>
                <intent-filter>
                    <action android:name="android.nfc.action.TECH_DISCOVERED" />
                </intent-filter>

                <meta-data
                    android:name="android.nfc.action.TECH_DISCOVERED"
                    android:resource="@xml/nfc_tech_filter" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChargeCard.ChargeCardActivity"
            android:label="@string/title_activity_charge_card"
            android:launchMode="singleTask">
        </activity>
    </application>

</manifest>
